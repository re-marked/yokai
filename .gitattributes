# Normalize line endings to LF in the repo; checkout as-is on all platforms
* text=auto eol=lf

# Force binary treatment for assets that should never be touched
*.png binary
*.jpg binary
*.gif binary
*.ico binary
